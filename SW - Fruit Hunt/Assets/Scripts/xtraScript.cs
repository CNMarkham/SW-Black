using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class xtraScript : MonoBehaviour
{
    public GameObject airTag;
    public GameObject bomb;
    // Start is called before the first frame update
    void Start()
    {
        //in the beginning of the game, spawn the airtag at this position and the bomb at that position
        //airTag.transform.position(7.319727, -25.18003, 0.03767954);
        //bomb.transform.position(-2.06, 2.769, 0.03767954);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
